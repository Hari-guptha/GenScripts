# Shared Types

This directory contains TypeScript types and interfaces that are shared between the frontend and backend applications.

## Usage

These types can be imported in both frontend and backend code to ensure type consistency across the application.
